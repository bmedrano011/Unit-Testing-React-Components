export const fetchItems = async () => {
  // Simulate fetching data from an API
  // For the sake of this example, we're using a mock response
  const mockResponse = ["Item A", "Item B"];
  return mockResponse;
};
